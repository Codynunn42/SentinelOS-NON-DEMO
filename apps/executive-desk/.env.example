# Receipt Ledger Backend Configuration

# Backend selection: 'memory', 'file', or 'postgres'
# Defaults to 'memory' (in-memory store for testing)
RECEIPT_LEDGER_BACKEND=memory

# For file backend: directory where receipts.jsonl is stored
# Defaults to '.data'
RECEIPT_LEDGER_DATA_DIR=.data

# For PostgreSQL backend: connection string
# Example: postgresql://user:password@localhost:5432/executive_desk
DATABASE_URL=

# Server Configuration
PORT=3000
NODE_ENV=development

# Authentication (optional for v1, required for production)
# If AUTH_ENABLED=true, /proxy/command requires Authorization: Bearer <token>
AUTH_ENABLED=false
# Preferred token for proxy auth
AUTH_BEARER_TOKEN=
# Legacy fallback for proxy auth if AUTH_BEARER_TOKEN is not set
JWT_SECRET=your-secret-key-here

# CORS Configuration
CORS_ORIGIN=http://localhost:3000,http://localhost:3001

# Sentinel AI Remote Connection (optional)
# When set, Executive Desk can probe and scan a hosted Sentinel AI service instead of a local process.
SENTINEL_AI_BASE_URL=
SENTINEL_AI_HEALTH_PATH=/health
SENTINEL_AI_SCAN_PATH=/scan
SENTINEL_AI_BEARER_TOKEN=
SENTINEL_AI_TIMEOUT_MS=5000
